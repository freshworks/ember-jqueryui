import jqueryUi from '../../../utils/jquery-ui';
import { module, test } from 'qunit';

module('Unit | Utility | jquery ui');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = jqueryUi();
  assert.ok(result);
});
